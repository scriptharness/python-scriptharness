* log
 * without mixins
* config
 * locking config
 * logging config
* mozharness.base.script
 * run_command
 * get_output_from_command
 * actions
  * allow for maven-type phases?  (if you run action X, it makes sure all actions pre-X run)
